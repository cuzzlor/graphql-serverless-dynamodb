{
    "graphql": {
        "debug": true,
        "tracing": true,
        "logTracing": true,
        "playground": {
            "endpoint": "/test/graphql"
        }
    },
    "logging": {
        "loggers": {
            "default": {
                "defaultMeta": { "environment": "test" }
            }
        }
    },
    "dynamodb": {
        "tableNamePrefix": "test_"
    }
}
