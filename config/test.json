{
    "graphql": {
        "debug": true,
        "tracing": true,
        "playground": {
            "endpoint": "/test/graphql"
        }
    },
    "logging": {
        "loggers": {
            "default": {
                "defaultMeta": { "environment": "test" }
            }
        }
    }
}
