query Movie($title: String!, $year: Int!) {
    movies(title: $title, year: $year) {
        title
        year
        info {
            directors
            releaseDate
            rating
            genres
            imageUrl
            plot
            rank
            runningTimeSecs
            actors
        }
    }
}
