{
    "schemaPath": "src/schema/schema.graphql",
    "includes": ["*.graphql"],
    "excludes": ["dist/**"],
    "extensions": {
        "endpoints": {
            "dev": {
                "url": "http://localhost:3000/graphql",
                "subscription": {
                    "url": "ws://localhost:3000/graphql"
                }
            }
        }
    }
}
